/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';

import { BaseService } from '../base-service';
import { ApiConfiguration } from '../api-configuration';
import { StrictHttpResponse } from '../strict-http-response';

import { DemoDataResultDto } from '../models/demo-data-result-dto';
import { seedDemoData } from '../fn/demo-data/seed-demo-data';
import { SeedDemoData$Params } from '../fn/demo-data/seed-demo-data';


/**
 * Seed presentation-ready demo tours and logs for the authenticated user.
 */
@Injectable({ providedIn: 'root' })
export class DemoDataService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http);
  }

  /** Path part for operation `seedDemoData()` */
  static readonly SeedDemoDataPath = '/api/demo-data/seed';

  /**
   * Replace all tours and logs for the authenticated user with demo data.
   *
   *
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `seedDemoData()` instead.
   *
   * This method doesn't expect any request body.
   */
  seedDemoData$Response(params?: SeedDemoData$Params, context?: HttpContext): Observable<StrictHttpResponse<DemoDataResultDto>> {
    const obs = seedDemoData(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * Replace all tours and logs for the authenticated user with demo data.
   *
   *
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `seedDemoData$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  seedDemoData(params?: SeedDemoData$Params, context?: HttpContext): Observable<DemoDataResultDto> {
    const resp = this.seedDemoData$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<DemoDataResultDto>): DemoDataResultDto => r.body)
    );
  }

}
