/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TourLogDto } from '../../models/tour-log-dto';

export interface ListLogs$Params {
  tourId: number;
}

export function listLogs(http: HttpClient, rootUrl: string, params: ListLogs$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TourLogDto>>> {
  const rb = new RequestBuilder(rootUrl, listLogs.PATH, 'get');
  if (params) {
    rb.path('tourId', params.tourId, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TourLogDto>>;
    })
  );
}

listLogs.PATH = '/api/tours/{tourId}/logs';
