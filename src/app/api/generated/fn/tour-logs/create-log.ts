/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateTourLogRequest } from '../../models/create-tour-log-request';
import { TourLogDto } from '../../models/tour-log-dto';

export interface CreateLog$Params {
  tourId: number;
      body: CreateTourLogRequest
}

export function createLog(http: HttpClient, rootUrl: string, params: CreateLog$Params, context?: HttpContext): Observable<StrictHttpResponse<TourLogDto>> {
  const rb = new RequestBuilder(rootUrl, createLog.PATH, 'post');
  if (params) {
    rb.path('tourId', params.tourId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TourLogDto>;
    })
  );
}

createLog.PATH = '/api/tours/{tourId}/logs';
