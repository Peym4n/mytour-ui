/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TourLogDto } from '../../models/tour-log-dto';

export interface GetLog$Params {
  tourId: number;
  logId: number;
}

export function getLog(http: HttpClient, rootUrl: string, params: GetLog$Params, context?: HttpContext): Observable<StrictHttpResponse<TourLogDto>> {
  const rb = new RequestBuilder(rootUrl, getLog.PATH, 'get');
  if (params) {
    rb.path('tourId', params.tourId, {});
    rb.path('logId', params.logId, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TourLogDto>;
    })
  );
}

getLog.PATH = '/api/tours/{tourId}/logs/{logId}';
