/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DemoDataResultDto } from '../../models/demo-data-result-dto';

export interface SeedDemoData$Params {
}

export function seedDemoData(http: HttpClient, rootUrl: string, params?: SeedDemoData$Params, context?: HttpContext): Observable<StrictHttpResponse<DemoDataResultDto>> {
  const rb = new RequestBuilder(rootUrl, seedDemoData.PATH, 'post');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DemoDataResultDto>;
    })
  );
}

seedDemoData.PATH = '/api/demo-data/seed';
