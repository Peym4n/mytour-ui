/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TourSuggestionDto } from '../../models/tour-suggestion-dto';

export interface SuggestTours$Params {
  q: string;
  limit?: number;
}

export function suggestTours(http: HttpClient, rootUrl: string, params: SuggestTours$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TourSuggestionDto>>> {
  const rb = new RequestBuilder(rootUrl, suggestTours.PATH, 'get');
  if (params) {
    rb.query('q', params.q, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TourSuggestionDto>>;
    })
  );
}

suggestTours.PATH = '/api/tours/suggestions';
