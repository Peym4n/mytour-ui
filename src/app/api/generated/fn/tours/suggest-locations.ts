/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LocationSuggestionDto } from '../../models/location-suggestion-dto';

export interface SuggestLocations$Params {
  q: string;
  limit?: number;
}

export function suggestLocations(http: HttpClient, rootUrl: string, params: SuggestLocations$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<LocationSuggestionDto>>> {
  const rb = new RequestBuilder(rootUrl, suggestLocations.PATH, 'get');
  if (params) {
    rb.query('q', params.q, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<LocationSuggestionDto>>;
    })
  );
}

suggestLocations.PATH = '/api/tours/location-suggestions';
