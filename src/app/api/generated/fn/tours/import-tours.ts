/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ImportResultDto } from '../../models/import-result-dto';
import { TourImportRequest } from '../../models/tour-import-request';

export interface ImportTours$Params {
      body: TourImportRequest
}

export function importTours(http: HttpClient, rootUrl: string, params: ImportTours$Params, context?: HttpContext): Observable<StrictHttpResponse<ImportResultDto>> {
  const rb = new RequestBuilder(rootUrl, importTours.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ImportResultDto>;
    })
  );
}

importTours.PATH = '/api/tours/import';
