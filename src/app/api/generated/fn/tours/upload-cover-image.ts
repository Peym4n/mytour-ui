/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CoverImageDto } from '../../models/cover-image-dto';

export interface UploadCoverImage$Params {
  tourId: number;
      body?: {
'file': Blob;
}
}

export function uploadCoverImage(http: HttpClient, rootUrl: string, params: UploadCoverImage$Params, context?: HttpContext): Observable<StrictHttpResponse<CoverImageDto>> {
  const rb = new RequestBuilder(rootUrl, uploadCoverImage.PATH, 'put');
  if (params) {
    rb.path('tourId', params.tourId, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CoverImageDto>;
    })
  );
}

uploadCoverImage.PATH = '/api/tours/{tourId}/cover-image';
