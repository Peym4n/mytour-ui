/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TourRouteDto } from '../../models/tour-route-dto';

export interface RefreshRoute$Params {
  tourId: number;
}

export function refreshRoute(http: HttpClient, rootUrl: string, params: RefreshRoute$Params, context?: HttpContext): Observable<StrictHttpResponse<TourRouteDto>> {
  const rb = new RequestBuilder(rootUrl, refreshRoute.PATH, 'post');
  if (params) {
    rb.path('tourId', params.tourId, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TourRouteDto>;
    })
  );
}

refreshRoute.PATH = '/api/tours/{tourId}/route/refresh';
